
<div>

  <%= render "shared/tilenav", user: @user %>

</div>

<%= current_user.own_photos.count %>

<ul>
  <% current_user.own_photos.each do |photo|%>
    <%= render "photos/photo", photo: photo %>
  <% end %>
</ul>
